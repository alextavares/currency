## Optional: OANDA polling (not required if you use the MT5 EA pusher)
OANDA_ENV=practice
OANDA_API_TOKEN=
OANDA_ACCOUNT_ID=

## Optional: polling interval (ms) if OANDA is enabled
FOREX_POLL_INTERVAL_MS=60000

